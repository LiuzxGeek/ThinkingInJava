package com.java.thinking.thread.cp;

public class FileChannelUtils {
public static void main(String[] args) {
    //FileChannel channel = null;
    //FileLock lock = null;
    //try {
    //  RandomAccessFile raf = new RandomAccessFile(jFile.getPath(), "rw");
    //  raf.seek(raf.length());//raf在文件末尾追加内容的处理
    //  channel = raf.getChannel();
    //  lock = channel.lock();//独占锁
	  //} catch (FileNotFoundException e) {
    //  Logger.t(TAG).e("获得上传文件 空：" + e.getMessage());
    //  return null;
    //} finally {
    //  if (lock != null) {
    //    try {
    //      lock.release();
    //    } catch (IOException e) {
    //      Logger.t(TAG).e("获得上传文件 lock：" + e.getMessage());
    //      return null;
    //    }
    //  }
    //  if (channel != null) {
    //    try {
    //      channel.close();
    //    } catch (IOException e) {
    //      Logger.t(TAG).e("获得上传文件 channel：" + e.getMessage());
    //      return null;
    //    }
    //  }
    //}
}
}
